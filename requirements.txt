# ==============================================================================
#  requirements.txt — Kripto Para Karar Destek Sistemi
#  Ahmet Yılmaz | Near East University
# ==============================================================================

# ── 1. Veri Toplama & API Bağlantıları ────────────────────────────────────────
requests>=2.31.0          # HTTP API istekleri (Binance & CoinGecko REST)
yfinance>=0.2.0           # Yahoo Finance verileri (Birincil veri kaynağı)
python-binance>=1.0.17    # Binance API client (Fallback veri kaynağı)

# ── 2. Veri İşleme & Analiz ───────────────────────────────────────────────────
pandas>=2.1.0             # Veri manipülasyonu ve önişleme
numpy>=1.26.0             # Matematiksel ve matris işlemleri
ta>=0.11.0                # Teknik analiz göstergeleri (RSI, MACD, BB vb.)

# ── 3. Makine Öğrenmesi & Derin Öğrenme ───────────────────────────────────────
scikit-learn>=1.3.0       # Sınıflandırıcılar (Random Forest) ve metrikler
xgboost>=2.0.0            # Gradient Boosting sınıflandırıcı
torch>=2.0.0              # LSTM/GRU Yapay Sinir Ağları (PyTorch)
joblib>=1.3.0             # Model kaydetme ve yükleme işlemleri

# ── 4. Arayüz & Görselleştirme ────────────────────────────────────────────────
streamlit>=1.28.0         # Karar destek web dashboard'u
plotly>=5.18.0            # İnteraktif grafikler ve ısı haritaları
matplotlib>=3.8.0         # Statik grafik çizimleri ve backtest çıktıları

# ── 5. Deneysel Pekiştirmeli Öğrenme ──────────────────────────────────────────
# RL bağımlılıkları ana pipeline için gerekli DEĞİLDİR.
# RL deneyleri için: pip install -r requirements-rl.txt
